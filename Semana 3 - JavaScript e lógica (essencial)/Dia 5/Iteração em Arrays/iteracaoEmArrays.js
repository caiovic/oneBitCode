/*

   as funções de iteração apresentadas na aula são HOF (de alta classe)
   enviamos callback(Funções que enviamos como parâmetro)
   O callback é chamado para cada elemento do array

  formato: 

  array.funcaoDeIterar(function(elementoAtual, indice, array)) {
    <corpo da função>
  })



  espera umcallback que receba como parâmetro o elemento atual, o índice 
  e o array completo
*/
