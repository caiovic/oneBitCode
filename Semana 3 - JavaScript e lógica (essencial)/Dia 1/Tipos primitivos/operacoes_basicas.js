// 1 + 3 = 4 no console (soma)

// 5 * 9 = 45 no console (multiplicação)

// 4.321 * 2.3 = 9.93829999... no console - não precisa colocar número inteiro (multiplicação)

// 6 / 2 = 3 no console (divisão)

// 8.5 / -5 = 1.7 no console (divisão)

// 10 % 3 = 1 no console (módulo - resto da divisão)

// "planeta" + "terra" = "planetaterra" no sonsole (concatenação - soma de duas strings)

// "Planeta " + "Terra" = "Planeta Terra" no console
// (concatenação - soma de duas strings (O espaço também é um valor))

// "Planeta Terra"[2] = "a" no console (Essa função é utilizada para pegar um caractere
// específico da streing, lembrando que usamos o 2 nessa questão pois sempre começa do zero)

// "Planeta Terra".length = 13 no console (Vê o tamanho da variável, incluindo espaços)

// "Planeta " + "Terra"[3] = "Planeta r" (pode juntar mais de uma operação)

// ("Planeta " + "Terra")[3] = "n" no console (Quando colocado entre aspas, tudo dentro tem
// prioridade)

// "Eu vivo na terra a " + 30 + "anos" = "Eu vivo na Terra a 30 anos" no console
// (Transformou o 30 em string devido a tipagem fraca)

// 5 * 5 + 1 = 26 no console (Ordem de precedência)

// 5 * (5 + 1) = 30 no console (Usa-se aspas para alterar a ordem de precedência)

// 5 * (5 / (5 - 4) + 1) = 30 no console (Usa-se aspas para alterar a ordem de precedência
// é possivel usar parênteses dentro de parênteses)
